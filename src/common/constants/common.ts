export enum Repository {
  USERS = 'USERS_REPO',
  ROOMS = 'ROOMS_REPO',
  ROOM_BOOKINGS = 'ROOM_BOOKINGS_REPO',
}

export enum Services {
  USERS = 'USERS_SERVICE',
  AUTH = 'AUTH_SERVICE',
  ROOMS = 'ROOMS_SERVICE',
  ROOM_BOOKINGS = 'ROOM_BOOKINGS_SERVICE',
  REDIS = 'REDIS_SERVICE',
}

export enum Routes {
  USERS = 'users',
  AUTH = 'auth',
  ROOMS = 'rooms',
  ROOM_BOOKINGS = 'room_bookings',
  REDIS = 'redis',
}
