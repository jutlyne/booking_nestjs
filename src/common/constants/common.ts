export enum Repository {
  USERS = 'USERS_REPO',
  ROOMS = 'ROOMS_REPO',
}

export enum Services {
  USERS = 'USERS_SERVICE',
  AUTH = 'AUTH_SERVICE',
  ROOMS = 'ROOMS_SERVICE',
}

export enum Routes {
  USERS = 'users',
  AUTH = 'auth',
  ROOMS = 'rooms',
}
